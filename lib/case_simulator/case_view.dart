// case_view.dart

import 'package:flutter/material.dart';

class CaseView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Case View')),
      body: Center(child: Text('')), // Blank for now
    );
  }
}
